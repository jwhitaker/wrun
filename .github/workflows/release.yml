name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build binaries
        run: |
          # Linux AMD64
          GOOS=linux GOARCH=amd64 go build -o watchrun-linux-amd64
          
          # Linux ARM64
          GOOS=linux GOARCH=arm64 go build -o watchrun-linux-arm64
          
          # macOS AMD64
          GOOS=darwin GOARCH=amd64 go build -o watchrun-darwin-amd64
          
          # macOS ARM64
          GOOS=darwin GOARCH=arm64 go build -o watchrun-darwin-arm64
          
          # Windows AMD64
          GOOS=windows GOARCH=amd64 go build -o watchrun-windows-amd64.exe
          
          # Windows ARM64
          GOOS=windows GOARCH=arm64 go build -o watchrun-windows-arm64.exe

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            watchrun-linux-amd64
            watchrun-linux-arm64
            watchrun-darwin-amd64
            watchrun-darwin-arm64
            watchrun-windows-amd64.exe
            watchrun-windows-arm64.exe
          generate_release_notes: true
